<wxs src="/pages/xiangxi/filter.wxs" module="filters"/>

<!-- 下拉菜单 -->
<van-dropdown-menu>
  <van-dropdown-item value="{{ value1 }}" options="{{ option1 }}" bind:change="try"/>
  <van-dropdown-item id="item" title="{{ itemTitle }}">
        <picker mode="multiSelector" bindchange="bindMultiPickerChange" bindcolumnchange="bindMultiPickerColumnChange" value="{{multiIndex}}" range="{{multiArray}}"> 
          <input placeholder='                                                                '/> 
          <input value='{{time}}' placeholder='                                                        选择时间'/> 
          <input  placeholder='                                                               '/> 
        </picker> 

</van-dropdown-item>
</van-dropdown-menu>

<!-- 商品卡片 -->
<block wx:key="item" wx:for="{{shuju}}" >
        <van-card data-id='{{item._id}}' bindtap="gotoresult" >
          <view slot="title" >
            {{item.name}}:
          </view>
          <view slot="desc" class='td'> {{item.content}} </view>
          <div slot="bottom">
            <van-tag plain type="danger">{{item.ifread}}</van-tag>
            <van-tag plain type="danger">{{item.ifstar}}</van-tag>
          </div>
          <view slot="footer">
            <text>{{filters.formatDate(item.time)}}</text>
          </view> 
      </van-card>
      <van-divider /> 
</block> 
